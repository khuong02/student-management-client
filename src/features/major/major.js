import { createSlice, createAsyncThunk } from "@reduxjs/toolkit";

const initialState = {};

const major = createSlice({
  name: "major",
  initialState,
  reducers: {},
});

export const {} = major.actions;
export default major.reducer;
